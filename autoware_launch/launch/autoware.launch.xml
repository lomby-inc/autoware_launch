<?xml version="1.0" encoding="UTF-8"?>
<launch>
  <!-- Essential parameters -->
  <arg name="map_path" description="point cloud and lanelet2 map directory path"/>
  <arg name="vehicle_model" default="sample_vehicle" description="vehicle model name"/>
  <arg name="sensor_model" default="sample_sensor_kit" description="sensor model name"/>
  <arg name="pointcloud_container_name" default="pointcloud_container"/>
  <arg name="data_path" default="$(env HOME)/autoware_data" description="packages data and artifacts directory path"/>

  <!-- launch module preset -->
  <arg name="planning_module_preset" default="default" description="planning module preset"/>

  <!-- Optional parameters -->
  <!-- Modules to be launched -->
  <arg name="launch_vehicle" default="true" description="launch vehicle"/>
  <arg name="launch_system" default="true" description="launch system"/>
  <arg name="launch_map" default="true" description="launch map"/>
  <arg name="launch_sensing" default="true" description="launch sensing"/>
  <arg name="launch_sensing_driver" default="true" description="launch sensing driver"/>
  <arg name="launch_localization" default="true" description="launch localization"/>
  <arg name="launch_perception" default="true" description="launch perception"/>
  <arg name="launch_planning" default="true" description="launch planning"/>
  <arg name="launch_control" default="true" description="launch control"/>
  <arg name="launch_api" default="true" description="launch api"/>
  <arg name="launch_stand_alone_systems" default="true" description="launch stand alone systems"/>
  <!-- Global parameters -->
  <arg name="use_sim_time" default="false" description="use_sim_time"/>
  <!-- Vehicle -->
  <arg name="vehicle_id" default="$(env VEHICLE_ID default)" description="vehicle specific ID"/>
  <arg name="launch_vehicle_interface" default="true" description="launch vehicle interface"/>
  <!-- Control -->
  <arg name="check_external_emergency_heartbeat" default="false"/>
  <!-- Map -->
  <arg name="lanelet2_map_file" default="lanelet2.osm" description="lanelet2 map file name"/>
  <arg name="pointcloud_map_file" default="map.pcd" description="pointcloud map file name"/>
  <arg name="pointcloud_map_metadata_file" default="pointcloud_map_metadata.yaml" description="pointcloud map metadata file name, relative to map_path. A path that does not exist makes map_loader derive metadata from a single PCD, which is the only way to load an untiled map"/>
  <!-- System -->
  <arg name="system_run_mode" default="online" description="run mode in system"/>
  <arg name="launch_system_monitor" default="false" description="launch system monitor"/>
  <arg name="launch_dummy_diag_publisher" default="false" description="launch dummy diag publisher"/>
  <arg name="diagnostic_graph_aggregator_graph_path" default="$(find-pkg-share autoware_launch_lomby)/config/system/diagnostic_graph_aggregator/autoware-main.yaml" description="diagnostic graph config"/>
  <!-- Tools -->
  <arg name="rviz" default="true" description="launch rviz"/>
  <arg name="rviz_config" default="$(find-pkg-share autoware_launch_lomby)/rviz/autoware.rviz" description="rviz config"/>
  <arg name="rviz_respawn" default="true"/>
  <!-- Perception -->
  <arg name="perception_mode" default="lidar" description="select perception mode. camera_lidar_radar_fusion, camera_lidar_fusion, lidar_radar_fusion, lidar, radar"/>
  <arg name="lidar_detection_model" default="centerpoint" description="lidar detector. options: `transfusion`, `centerpoint`, `apollo`, `pointpainting`, `clustering`. `clustering` is rule-based and needs no CUDA/TensorRT"/>
  <arg name="use_object_filter" default="true" description="filter detected objects against the lanelet map"/>
  <arg name="use_moving_object_filter" default="false" description="republish only tracked objects that are actually moving, on .../tracking/moving_objects. Opt-in: it is a noise reducer, not a safety filter -- a standing pedestrian has zero speed and is deliberately absent"/>
  <arg name="pointcloud_map_filter_param_file" default="$(find-pkg-share autoware_launch_lomby)/config/perception/object_recognition/detection/pointcloud_filter/pointcloud_map_filter.param.yaml" description="compare_map_segmentation params, which mask mapped structure out of the clustering input"/>
  <arg name="traffic_light_recognition/enable_fine_detection" default="true" description="enable traffic light fine detection"/>
  <arg name="image_raw0" default="/sensing/trafficlight_camera/image_rect" description="image raw topic name"/>
  <arg name="camera_info0" default="/sensing/trafficlight_camera/camera_info" description="camera info topic name"/>
  <!-- Auto mode setting-->
  <arg name="enable_all_modules_auto_mode" default="false" description="enable all module's auto mode"/>
  <arg name="is_simulation" default="false" description="Autoware's behavior will change depending on whether this is a simulation or not."/>

  <!-- Global parameters -->
  <group scoped="false">
    <include file="$(find-pkg-share global_parameter_loader)/launch/global_params.launch.py">
      <arg name="use_sim_time" value="$(var use_sim_time)"/>
      <arg name="vehicle_model" value="$(var vehicle_model)"/>
    </include>
  </group>

  <!-- Pointcloud container -->
    <include file="$(find-pkg-share autoware_launch_lomby)/launch/pointcloud_container.launch.py">
      <arg name="use_multithread" value="true"/>
      <arg name="container_name" value="$(var pointcloud_container_name)"/>
    </include>

  <!-- Vehicle -->
  <group if="$(var launch_vehicle)">
    <include file="$(find-pkg-share tier4_vehicle_launch)/launch/vehicle.launch.xml">
      <arg name="vehicle_model" value="$(var vehicle_model)"/>
      <arg name="sensor_model" value="$(var sensor_model)"/>
      <arg name="vehicle_id" value="$(var vehicle_id)"/>
      <arg name="launch_vehicle_interface" value="$(var launch_vehicle_interface)"/>
      <arg name="config_dir" value="$(find-pkg-share lomby_individual_params)/config/$(var vehicle_id)/$(var sensor_model)"/>
      <arg name="raw_vehicle_cmd_converter_param_path" value="$(find-pkg-share autoware_launch_lomby)/config/vehicle/raw_vehicle_cmd_converter/raw_vehicle_cmd_converter.param.yaml"/>
    </include>
  </group>

   <!-- System -->
  <group if="$(var launch_system)">
    <include file="$(find-pkg-share autoware_launch_lomby)/launch/components/lomby_system_component.launch.xml"/>
  </group>

  <!-- Map -->
  <group if="$(var launch_map)">
    <include file="$(find-pkg-share autoware_launch_lomby)/launch/components/lomby_map_component.launch.xml">
      <!-- Select Vector map node. lanelet, linestring -->
      <arg name="vector_map_type" value="linestring"/>
    </include>
  </group>

  <!-- Sensing -->
  <group if="$(var launch_sensing)">
    <include file="$(find-pkg-share autoware_launch_lomby)/launch/components/lomby_sensing_component.launch.xml"/>
  </group>

  <!-- Localization -->
  <group if="$(var launch_localization)">
    <include file="$(find-pkg-share autoware_launch_lomby)/launch/components/lomby_localization_component.launch.xml">
      <!-- select pose_estimator: ndt, yabloc, eagleye -->
      <arg name="pose_source" value="ndt"/>
      <!-- select twist_estimator. gyro_odom, eagleye -->
      <arg name="twist_source" value="gyro_odom"/>
      <!-- The topic will be used in the localization util module -->
      <arg name="input_pointcloud" value="/sensing/lidar/concatenated/pointcloud"/>
    </include>
  </group>

  <!-- Perception -->
  <group if="$(var launch_perception)">
    <include file="$(find-pkg-share autoware_launch_lomby)/launch/components/lomby_perception_component.launch.xml">
      <arg name="data_path" value="$(var data_path)"/>
      <arg name="lidar_detection_model" value="$(var lidar_detection_model)"/>
      <arg name="use_object_filter" value="$(var use_object_filter)"/>
      <arg name="pointcloud_map_filter_param_file" value="$(var pointcloud_map_filter_param_file)"/>
      <arg name="use_moving_object_filter" value="$(var use_moving_object_filter)"/>
      <arg name="use_traffic_light_recognition" value="false"/>
      <arg name="use_empty_dynamic_object_publisher" value="true"/>
      <arg name="is_simulation" value="$(var is_simulation)"/>
    </include>
  </group>

  <!-- Planning -->
  <group if="$(var launch_planning)">
    <include file="$(find-pkg-share autoware_launch_lomby)/launch/components/lomby_planning_component.launch.xml">
      <arg name="module_preset" value="$(var planning_module_preset)"/>
      <arg name="enable_all_modules_auto_mode" value="$(var enable_all_modules_auto_mode)"/>
      <arg name="is_simulation" value="$(var is_simulation)"/>
      <!-- Select global planning mode. autoware, nav2_planning -->
      <arg name="global_planning" value="nav2_planning"/>
      <!-- Select behavior component mode. autoware, nav2_bt -->
      <arg name="behavior_component" value="nav2_bt"/>
      <!-- Select local planning mode. autoware, nav2_controller -->
      <arg name="local_planning" value="nav2_controller"/>
    </include>
  </group>

  <!-- Control -->
  <group if="$(var launch_control)">
    <include file="$(find-pkg-share autoware_launch_lomby)/launch/components/lomby_control_component.launch.xml"/>
  </group>

  <!-- API -->


  <!-- Tools -->
  <group>
    <node
      pkg="rviz2"
      exec="rviz2"
      name="rviz2"
      output="screen"
      args="-d $(var rviz_config) -s $(find-pkg-share autoware_launch_lomby)/rviz/image/autoware.png"
      if="$(var rviz)"
      respawn="$(var rviz_respawn)"
    >
      <env name="QT_QPA_PLATFORMTHEME" value="qt5ct"/>
    </node>
  </group>

  <group if="$(eval &quot;'$(var launch_stand_alone_systems)' == 'true' and '$(var is_simulation)' == 'false'&quot;)">
    <include file="$(find-pkg-share autoware_launch_lomby)/launch/components/lomby_autoware_api_component.launch.xml"/>
  </group>
</launch>
